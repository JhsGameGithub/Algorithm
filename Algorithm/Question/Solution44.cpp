#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
//마구간 정하기(이분검색 응용)
int main(int argc, char* argv[])
{
	int n, c;

	cin >> n >> c;

	vector<int> x(n);

	for (int i = 0; i < n; i++)
		cin >> x[i];
	
	sort(x.begin(), x.end());

	int last = x[0], tmp, index;

	while()

	return 0;
}