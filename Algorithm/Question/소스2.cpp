#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char argv[])
{
	int n;
	cin >> n;
	int map[20][20] = { 0, };
	for (int i = 0; i < n; i++)
		for (int j = 0; j < n; j++)
			cin >> map[i][j];

	
			
	return 0;
}